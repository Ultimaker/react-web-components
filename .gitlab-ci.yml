image: node:8

stages:
  - test
  - deploy

before_script:
  - yarn install

test:
  stage: test
  script:
    # TODO: run actual tests here when we have them, but for now we just build storybook to test
    - yarn build
  except:
    - master
    # for testing only
    - small-popup-fixes

pages:
  stage: deploy
  script:
    - yarn build
  artifacts:
    paths:
    - public
  only:
    - master
    # for testing only
    - small-popup-fixes
