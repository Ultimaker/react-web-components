import * as ts from 'typescript';
export interface ICommentOptions {
    regex?: RegExp;
    otherLineLeading?: boolean;
    sameLineLeading?: boolean;
    sameLineTrailing?: boolean;
}
export declare abstract class JsCommentUtils {
    static extractComments(callExpression: ts.CallExpression, sourceFile: ts.SourceFile, commentOptions?: ICommentOptions): string[];
    private static getExtractionPositions(node, sourceFile);
    private static nodeIsOnSeparateLine(node, nodes, sourceFile);
    private static extractCommentsAtPosition(source, position, edge, commentOptions);
    private static defaultCommentOptions(options?);
    private static extractLineComment(source);
    private static extractBlockComment(source);
}
